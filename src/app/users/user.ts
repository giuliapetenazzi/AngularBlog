/** User class:
 * even if user manage is not required, this class helps in the login phase,
 * in the management of Remember me flag and in retrieving the user's name in the add comment operation */
export class User {
    username: string;
    password: string;
}